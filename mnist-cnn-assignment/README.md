# MNIST CNN Assignment

This folder contains the notebook used to fine-tune a pre-trained convolutional neural network on the MNIST dataset.

The trained model is saved as:

```python
mnist_resnet18.pth
